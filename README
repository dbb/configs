### dbbolton's configs

This git repository is a storage facility for my various config files. I plan to put the intended purpose for each file in its name. For example, "$HOME/.config/openbox/rc.xml" is simply called "openbox-rc.xml".


